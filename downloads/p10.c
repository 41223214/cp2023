#include <stdio.h>

int main() 
{
    int x = 25, y = 15, result; // 使用固定數值 25 和 15

    result = x * y; // 計算乘積

    // 印出乘積
    printf("兩個整數 %d 和 %d 的乘積 = %d\n", x, y, result);

    return 0; // 表示程式執行成功
}
